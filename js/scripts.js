var nonnumber = function(nonnumber) {
  if (nonnumber <= 0); {
    return false;
  }
};
var number = function(number) {
 if (number % 3 === 0); {
  return "ping";

} if (number % 5 === 0); {
  return "pong";
}
};

//   return false;
// };
// if (number % 3 === 0) {
// return "ping";
// } else {
  // (number <= 0);
